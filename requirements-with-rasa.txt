# EXPERIMENTAL: Requirements WITH Rasa - VERY DIFFICULT TO INSTALL
# WARNING: This may not work due to fundamental conflicts between Rasa and modern packages
# Rasa requires pydantic v1.x, but aiogram 3.x requires pydantic v2.x

# The fundamental conflict:
# - rasa 3.6.13 needs pydantic < 2.0.0
# - aiogram 3.2.0 needs pydantic >= 2.4.1
# - fastapi 0.104.1 needs pydantic >= 1.7.4, != 2.0.0, != 2.0.1, != 2.1.0

# SOLUTION: Use much older versions (may break functionality)

# Rasa (requires old pydantic)
rasa==3.6.13
rasa-sdk==3.6.2

# Use OLDER versions of everything else
fastapi==0.68.0
aiogram==2.25.1  # DOWNGRADE to aiogram v2!
uvicorn[standard]==0.15.0

# Database (compatible with Rasa)
sqlalchemy==1.4.53
alembic==1.8.1

# File and HTTP handling (older versions)
aiosqlite==0.17.0
psycopg2-binary==2.9.9
openpyxl==3.0.10
Pillow==9.5.0
python-multipart==0.0.5
aiofiles==0.8.0
httpx==0.24.1

# The key: old pydantic that Rasa needs
pydantic==1.10.12
python-dotenv==0.19.0
loguru==0.6.0
gunicorn==20.1.0

# IMPORTANT: This uses aiogram v2, not v3!
# The bot code will need major changes to work with this setup
# For 99% of users: just use requirements.txt (without Rasa)
